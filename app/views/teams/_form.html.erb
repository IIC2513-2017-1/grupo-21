<%= form_for(team) do |f| %>
  <% if team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre, :required => true %>
  </div>

  <div class="field">
    <%= f.label :pais %>
    <%= f.text_field :pais, :required => true %>
  </div>

  <div class="field">
    <%= f.label :avatar %>
    <%= f.file_field :avatar, :class=>"button-generic" %>
  </div>



  <div class="actions">
    <%= f.submit 'Crear Equipo', :class=>"button-generic"%>
  </div>
<% end %>
