<h1>Editar Partido</h1>

<%= render 'form', match: @match %>

<%= link_to 'Mostrar información', tournament_match_path(@tournament, @match), :class=>"button-generic" %> |
<%= link_to 'Volver', tournament_matches_path(@tournament.id), :class=>"button-generic" %>
